# Caddy reverse proxy — automatic HTTPS (Let's Encrypt) for natten-home.
# Domain comes from the environment (see deploy.env.example).
# Want cert-expiry notices? Add a global block:  { email you@example.com }

{$NATTEN_HOME_DOMAIN} {
	encode zstd gzip
	reverse_proxy app:8080
}
